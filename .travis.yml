language: cpp

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install valgrind
  
compiler:
  - gcc

before_script:
  - cd src

script: 
  - make
  - make MODE=DEBUG
  - make test
  - cd ..
  - python2.7 setup.py build
  - python3.4 setup.py build
