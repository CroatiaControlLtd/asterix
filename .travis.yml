language: cpp

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install valgrind
  
compiler:
  - gcc

before_script:
  - cd src

script: 
  - make
  - make MODE=DEBUG
  - make test
  - cd ..
  - mkdir dist-packages
  - python setup.py install --install-base=./dist-packages
